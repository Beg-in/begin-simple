{
  "name": "replace-me",
  "version": "0.0.1",
  "description": "replace-me",
  "main": "index.js",
  "author": "",
  "license": "",
  "scripts": {
    "clean": "rimraf docs",
    "autoprefixer": "postcss -u autoprefixer -r docs/styles/*.css; postcss -u autoprefixer -r docs/styles/**/*.css",
    "cssnano": "postcss -u cssnano -r docs/styles/*.css; postcss -u cssnano -r docs/styles/**/*.css",
    "css": "rsync -r -I --include=\"*/\" --include=\"*.css\" --exclude=\"*\" src/styles docs",
    "sass": "node-sass --indented-syntax true --output-style compressed -o docs/styles src/styles",
    "uglify": "mkdirp docs/scripts -p && uglifyjs src/scripts/*.js -m -o docs/js/app.js && uglifyjs src/scripts/*.js -m -c -o docs/scripts/app.min.js",
    "imagemin": "imagemin src/images/**/*.{png,jpg,jpeg,gif,ico} -o docs/images",
    "svgmin": "svgo -f src/images/**/*.svg -o docs/images/svgmin",
    "serve": "browser-sync start --server --files \"docs/css/*.css, docs/scripts/*.js, **/*.html, !node_modules/**/*.html\"",
    "build:css": "run-s sass css autoprefixer cssnano",
    "build:js": "run-s uglify",
    "build:images": "run-s imagemin svgmin",
    "build": "run-s build:*",
    "watch:css": "onchange \"src/styles\" -- run-s build:css",
    "watch:js": "onchange \"src/scripts\" -- run-s build:js",
    "watch:images": "onchange \"src/images\" -- run-s build:images",
    "watch": "run-p serve watch:*"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.5",
    "browser-sync": "^2.18.13",
    "cssnano": "^4.0.0-rc.2",
    "imagemin-cli": "^3.0.0",
    "mkdirp": "^0.5.1",
    "node-sass": "^4.5.3",
    "npm-run-all": "^4.1.1",
    "onchange": "^3.2.1",
    "postcss-cli": "^4.1.1",
    "rimraf": "^2.6.2",
    "svgo": "^0.7.2",
    "uglify-js": "^3.1.3"
  },
  "dependencies": {
    "frow": "2.0.1",
    "letsgo": "0.2.3"
  }
}
